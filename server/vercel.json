{
  "version": 2,
  "builds": [
    {
      "src": "server/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server/index.js"
    }
  ],
  "buildCommand": "bun install --prefix server && bun install --prefix frontend && bun run build --prefix frontend"
}
